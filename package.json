{
  "name": "elenextstart",
  "version": "1.0.0",
  "main": "main/index.js",
  "license": "MIT",
  "scripts": {
    "dev": "next renderer",
    "build": "next build renderer",
    "export": "yarn build && npx next export renderer",
    "start": "electron .",
    "dist": "yarn export && electron-builder"
  },
  "devDependencies": {
    "electron": "^4.0.1",
    "electron-builder": "^20.38.4",
    "electron-is-dev": "^1.0.1"
  },
  "dependencies": {
    "next": "^7.0.2",
    "electron-next": "^3.1.5",
    "react": "^16.8.0-alpha.0",
    "react-dom": "^16.8.0-alpha.0"
  }
}
